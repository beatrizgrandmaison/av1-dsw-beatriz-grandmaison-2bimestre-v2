
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")

}

// Modelo de Quadra
model Quadra {
  id        Int     @id @default(autoincrement())
  nome      String
  tipo      String
  valorHora Float
}  